version: 1.0
runtime: python3 
build:
  commands:
    pre-build:
      - apt install git -y
    build:
      - pip install -r requirements_web.txt
run: 
  command: python src/index.py
  network: 
    port: 8000
